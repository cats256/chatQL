import { LogoutButton } from "../buttons";

interface IChatProps {}

const Chat: React.FC<IChatProps> = () => {
    return (
        <div>
            <LogoutButton />
        </div>
    );
};

export default Chat;
